import { defineComponent, h } from "vue";
import { useThemeLocaleData } from "@theme-hope/composables";
import SidebarLinks from "@theme-hope/module/sidebar/components/SidebarLinks";
import { useSidebarItems } from "@theme-hope/module/sidebar/composables";
import "../styles/sidebar.scss";
export default defineComponent({
    name: "SideBar",
    setup(_props, { slots }) {
        const themeLocale = useThemeLocaleData();
        const sidebarItems = useSidebarItems();
        return () => {
            var _a, _b, _c;
            return h("aside", { class: ["sidebar", { "hide-icon": !themeLocale.value.sidebarIcon }] }, [
                (_a = slots.top) === null || _a === void 0 ? void 0 : _a.call(slots),
                ((_b = slots.default) === null || _b === void 0 ? void 0 : _b.call(slots)) || h(SidebarLinks, { config: sidebarItems.value }),
                (_c = slots.bottom) === null || _c === void 0 ? void 0 : _c.call(slots),
            ]);
        };
    },
});
//# sourceMappingURL=Sidebar.js.map