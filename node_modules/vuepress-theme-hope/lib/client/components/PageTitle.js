import { usePageData, usePageFrontmatter } from "@vuepress/client";
import { defineComponent, h, resolveComponent } from "vue";
import { useIconPrefix, usePageInfo, useThemeLocaleData, } from "@theme-hope/composables";
import "../styles/page-title.scss";
export default defineComponent({
    name: "PageTitle",
    setup() {
        const page = usePageData();
        const frontmatter = usePageFrontmatter();
        const pageInfoProps = usePageInfo();
        const themeLocale = useThemeLocaleData();
        const iconPrefix = useIconPrefix();
        return () => h("div", { class: "page-title" }, [
            h("h1", [
                themeLocale.value.titleIcon !== false && frontmatter.value.icon
                    ? h("i", {
                        class: ["icon", `${iconPrefix.value}${frontmatter.value.icon}`],
                    })
                    : null,
                page.value.title,
            ]),
            h(resolveComponent("ArticleInfo"), { ...pageInfoProps }),
            h("hr"),
        ]);
    },
});
//# sourceMappingURL=PageTitle.js.map